<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/action_searchBtn"
        android:icon="@mipmap/search_icon"
        android:title="@string/search_btn"
        app:showAsAction="always" />
    <item
        android:id="@+id/Account_settingBtn"
        android:title="@string/account_settingBtn" />
    <item
        android:id="@+id/main_logoutBtn"
        android:title="@string/action_logoutBtn" />
</menu>